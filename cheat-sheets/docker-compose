docker-compose up	// use docker-compose.yml
docker-compose up --build // force build images before starting containers

docker-compose build // build the images (included inside the `up --build` option)

docker-compose -f [filename.yml] // use custom yml files

